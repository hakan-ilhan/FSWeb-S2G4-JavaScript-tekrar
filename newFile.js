const { sayilar, enkucuk, enbuyuk } = require(".");

for (i = 0; i < sayilar.length; i++) {
    (); if (sayilar[i] < enkucuk) {
        enkucuk = sayilar[i];
    }
    (); if (sayilar[i] > enbuyuk) {
        enbuyuk = sayilar[i];
    }

}
